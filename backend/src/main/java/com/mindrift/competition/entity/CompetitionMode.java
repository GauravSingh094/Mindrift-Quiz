package com.mindrift.competition.entity;

public enum CompetitionMode {
    STANDARD,   // Normal quiz scoring
    SPEED,      // Time bonus for fast answers
    SURVIVAL    // Elimination on wrong answer
}

package com.mindrift.competition.entity;

public enum CompetitionType {
    OPEN,         // Anyone can join
    PRIVATE,      // Join code required
    INVITE_ONLY   // Organizer sends invitations
}
